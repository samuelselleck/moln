type MyStruct = '{}

fn test_func(namespace: MyStruct, takeout: i64) -> i64 {
    let something: i64 = 5;
	func_is_valid(5 + 23, true || false && false != !(3 >= 4));
	{
		fn func_is_valid(i: i64) -> str {
			"hello world"
		};
		let test = 5;
		let something_else = test + 3;
		func_is_valid(something_else + test);
	};
	if true { something + 6 } else { 5 };
	3 + 3
}

fn func_is_valid(i: i64, b: bool) -> str {
	"hello world"
}
